appId: com.vyra.studio
productName: VYRA Studio
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!docs/*'
asarUnpack:
  - resources/**
  - node_modules/ffmpeg-static/**
win:
  icon: build/icon.ico
  executableName: VYRA Studio
nsis:
  artifactName: ${productName}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  icon: build/icon.png
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    NSCameraUsageDescription: VYRA Studio requests access to the device's camera.
    NSMicrophoneUsageDescription: VYRA Studio requests access to the device's microphone for recording audio.
    NSScreenCaptureUsageDescription: VYRA Studio requests access to capture the screen for recording.
    NSDocumentsFolderUsageDescription: VYRA Studio requests access to the user's Documents folder.
    NSDownloadsFolderUsageDescription: VYRA Studio requests access to the user's Downloads folder.
  notarize: true
dmg:
  artifactName: ${productName}-${version}.${ext}
linux:
  target:
    - AppImage
    - deb
  icon: build/icon.png
  maintainer: Keybachira
  vendor: VYRA Studio
  category: Utility
  desktop:
    Name: VYRA Studio
    Comment: Your camera. Your space.
    Keywords: camera;recording;overlay;studio;
    StartupWMClass: VYRA Studio
snap:
  plugs:
    - browser-support
    - desktop
    - desktop-legacy
    - home
    - pulseaudio
    - unity7
    - upower-observe
    - wayland
    - x11
    - network
    - network-bind
    - opengl
    - camera
    - audio-record
appImage:
  artifactName: ${productName}-${version}.${ext}
npmRebuild: false
releaseInfo:
  releaseNotesFile: build/release-notes.md
